# Kernel Visualization

Step through kernels, not print statements.
Write C/CUDA, step through it line by line, and watch every thread, loop, and buffer update in a live 3D view. No more `printf` debugging matmuls.

## Stack
Next.js, TypeScript, CodeMirror, three.js.
